# DELIS — Итоговый отчёт за сессию

**Дата:** 2026-08-10
**Ветка:** `arena/019feafa-delis-tg`
**Коммитов за сессию:** 10
**Изменено файлов:** 54 (≈1097 строк добавлено, 382 удалено)

---

## 1. Новые фичи (10)

### 🛒 Продажи / конверсия
| Фича | Что делает |
|---|---|
| **Промокод в брошенной корзине** | Бот напоминает о забытой корзине через 2 часа + даёт персональный одноразовый код `BACK-XXXXXX` на 10% (7 дней). Код привязан к пользователю, «сгорает» после использования. |
| **«Купить в 1 клик»** | Кнопка на шаге корзины — заказ в один тап (самовывоз с завода, имя/телефон из Telegram, «как можно скорее»). Если нет телефона — ведёт на шаг доставки. |
| **«Вернитесь через N дней»** | После доставки заказа бот через ~30 дней напоминает докупить каждый расходник (кнопка ведёт прямо на товар). Одно напоминание на (клиент, товар). |
| **Экспорт клиентов в CSV** | Во вкладке «Клиенты» админки — кнопка выгрузки: имя, телефоны, Telegram/ID, число заказов, сумма, история покупок. Готово для Excel. |

### 👤 Лояльность / удержание
| Фича | Что делает |
|---|---|
| **Истории — как в Instagram** | Истории живут **24 часа** и удаляются автоматически (сервер чистит каждый час). Клиент может **удалить свою историю** сам (кнопка 🗑 в просмотре), админ — во вкладке Stories. |

### ⚙️ Удобство / техника
| Фича | Что делает |
|---|---|
| **Офлайн-бейдж** | При обрыве интернета сверху появляется плашка «Вы офлайн — данные могут быть не свежими» (uz/ru/en), исчезает при восстановлении. |
| **Фикс скроллов** | Добавлен счётчик блокировки `lockScroll/unlockScroll` — вложенные окна больше не «дерутся» за прокрутку фона. Применено к шторкам, экрану товара, «Спасибо». |

### 🎨 Дизайн
| Фича | Что делает |
|---|---|
| **Экран «Rahmat»** | Праздничный: конфетти, анимированная галочка, карточка суммы, кэшбэк Stars, таймлайн, кнопки внизу (не наезжают на контент). |
| **Кнопки товара** | Чистые сплошные кнопки «Добавить в корзину» / «Перейти в корзину» — простой проверенный стиль. |
| **Экран товара** | Картинка теперь уезжает при скролле (премиальная светлая карточка вместо мрачного hero). |

### 📄 Документация
| Что | Детали |
|---|---|
| **Карта проекта** | `STRUCTURE.md` — что/где/зачем лежит, вес проекта, правила «что куда класть». |
| **Комментарии** | Русская подпись-заголовок добавлена в каждый файл `src/` и `server/src/` (там, где не было). |

---

## 2. Доставка и BTS — правки
- Проверено: админ-вкладка «Доставка» **уже позволяет менять** курьер + BTS для **всех 14 регионов** и порог бесплатной доставки (сохраняется на сервер).
- **Исправлен баг**: в корзине было захардкожено «доставка от 12000» — теперь показывается **реальная цена из настроек админки**.

---

## 3. Что уже было (проверено, не дублировалось)
- Автоматические поздравления с днём рождения (бот + код `BDAY10`)
- Уведомления о статусе заказа через бота
- Ежедневный отчёт + бэкап в Telegram админу
- Мгновенное уведомление админу о новом заказе
- Тёмная тема
- Анимация корзины (бейдж «прыгает»)

---

## 4. Технические проверки
| Проверка | Результат |
|---|---|
| Фронтенд-сборка (`vite build`) | ✅ Собирается |
| Фронтенд-тесты (`data.test.ts`) | ✅ 15 passed |
| Серверные тесты | ✅ 94 passed |
| Оба сервера (dev + API) | ✅ Работают (200) |
| Git-рабочая директория | ✅ Чистая, всё закоммичено |

---

## 5. Вес проекта
| Часть | Размер |
|---|---|
| Исходники (без node_modules/dist) | ~3,9 МБ |
| Финальный бандл (`index.html`) | ~1,145 МБ (gzip ~325 КБ) |
| node_modules (фронт + сервер) | ~110 + 79 МБ |

---

## 6. Примечания
- Напоминания бота (брошенная корзина, «вернитесь через N дней», день рождения) заработают в продакшене при указании реального **`TG_BOT_TOKEN`** в `server/.env` (в песочнице его нет, поэтому бот пропущен).
- Персональные промокоды (`BACK-*`, `ST-*`) валидируются на сервере (проверка владельца по tg_id) — подделать чужой код нельзя.
