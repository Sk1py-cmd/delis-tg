# ✅ Как увидеть изменения из CHANGES_REPORT.md в запущенном превью

Все правки из отчёта **применились и работают** — и в коде, и в production-сборке (gh-pages).
Проблема в том, что почти все фичи — **интерактивные**: они появляются не на главном
экране, а на конкретных экранах или при определённых действиях.

Ниже — где и как каждую фичу увидеть в запущенном превью (порт 5173).

---

## 🛒 Видны сразу (на экране товара / в каталоге)

| Фича | Где увидеть |
|---|---|
| **Кнопки товара** — чистые сплошные кнопки | Открой любой товар (например «Graphite Wax») → снизу две кнопки «Добавить в корзину» / «Перейти в корзину» |
| **Экран товара** — картинка уезжает при скролле, светлая карточка | Открой товар и проскролль — фото плавно уезжает вверх (был мрачный hero) |
| **Фикс скроллов** | Открой товар → окно не «дерёт» прокрутку фона при закрытии |

## 🛒 В корзине / при оформлении

| Фича | Где увидеть |
|---|---|
| **«Купить в 1 клик»** ⚡ | Добавь товар → корзина → кнопка «⚡ Купить в 1 клик» (заказ в один тап) |
| **Реальная цена доставки** | В корзине показывается фактическая цена курьера/BTS из настроек админки, а не «от 12000» |

## 🎉 После оформления заказа

| Фича | Где увидеть |
|---|---|
| **Экран «Rahmat»** | Оформи заказ → праздничный экран: конфетти, анимированная галочка, карточка суммы, кэшбэк Stars, таймлайн, кнопки внизу (не наезжают на контент) |

## 📡 Условия / фон / админка

| Фича | Где увидеть |
|---|---|
| **Офлайн-бейдж** | Отключи интернет (DevTools → Network → Offline) → сверху плашка «Вы офлайн…» (uz/ru/en), исчезает при восстановлении |
| **CSV-экспорт клиентов** | Админка → вкладка «Клиенты» → кнопка выгрузки (имя, телефон, заказы, сумма, история) |
| **Промокод в брошенной корзине** `BACK-*` | Нужен **реальный `TG_BOT_TOKEN`** в `server/.env` — бот напомнит о корзине через 2 ч и даст личный код на 10% |
| **«Вернитесь через N дней»** | Нужен `TG_BOT_TOKEN` — через ~30 дней после доставки бот напоминает докупить расходники |
| **24-часовые истории** | Истории живут 24 ч и удаляются сами; клиент удаляет свою историю кнопкой 🗑 |

---

## ⚠️ Почему часть фич «не видна» сразу

- Фичи с **ботом** (брошенная корзина, «вернитесь через N дней», день рождения) требуют
  **`TG_BOT_TOKEN`** — в песочнице его нет, поэтому бот пропущен (лог: `TG_BOT_TOKEN not set — bot skipped`). Это **не значит, что фичи не сделаны** — код на месте.
- Фичи в **Telegram Mini App** частью зависят от `initData` (в обычном браузере его нет),
  но витрина, корзина, каталог и demo-покупки работают полностью.

---

## ✅ Полный чеклист проверок (выполнено 11.08.2026)

Все **12 фич** из `CHANGES_REPORT.md` проверены в коде — каждая присутствует:

| # | Фича | Где в коде | Статус |
|---|---|---|---|
| 1 | Промокод `BACK-*` в брошенной корзине | `server/src/bot.ts` | ✅ |
| 2 | «Купить в 1 клик» ⚡ | `src/checkout-modal.tsx` | ✅ |
| 3 | «Вернитесь через N дней» | `server/src/bot.ts` (reorder_reminders) | ✅ |
| 4 | CSV-экспорт клиентов | `src/features-admin.tsx`, `src/api.ts` | ✅ |
| 5 | Истории 24ч + удаление своей | `server/src/index.ts` | ✅ |
| 6 | Офлайн-бейдж | `src/App.tsx` | ✅ |
| 7 | Экран «Rahmat» (конфетти, галочка, кэшбэк) | `src/thank-you.tsx` | ✅ |
| 8 | Кнопки товара (сплошные) | `src/screen-product.tsx` | ✅ |
| 9 | Экран товара (картинка уезжает) | `src/screen-product.tsx` | ✅ |
| 10 | Фикс скроллов (lockScroll/unlockScroll) | `src/kit.tsx` + экраны | ✅ |
| 11 | Админ-вкладка «Доставка» (14 регионов + порог) | `src/features-admin.tsx` (DeliveryAdminTab) | ✅ |
| 12 | Реальная цена доставки из настроек | `src/checkout-modal.tsx` (getRegionTariff) | ✅ |

## 🔧 Технические проверки (перезапущено и пересобрано)

| Проверка | Результат |
|---|---|
| Сборка фронтенда (`vite build`) | ✅ `dist/index.html` 1.17 МБ (gzip 326 КБ) |
| Фронтенд-тесты (`data.test.ts`) | ✅ **15 passed** |
| Серверные тесты | ✅ **94 passed** |
| Фронтенд-сервер `:5173` | ✅ HTTP 200 |
| Бэкенд-API `:3001` | ✅ `/health` ok, БД засеяна 8 товарами |
| Связка фронт→бэкенд через vite-прокси | ✅ `/v1/products` → 8 товаров |

> Примечание: `node_modules/` в песочнице не сохраняется между сессиями, поэтому
> после каждого перезапуска зависимости переустанавливаются заново (для `better-sqlite3`
> — компиляция из системных заголовков: `npm_config_nodedir=/usr/local`).

## ✅ Production-сборка

- Изменения есть в коде (`grep` по `BACK-`, `one_click`, `offline`, `Rahmat`, CSV и т.д.) — ✅
- Production-сборка `gh-pages` содержит все фичи (CSV, Rahmat, offline, one click, «Купить в 1 клик», экспорт) — ✅
- Фронтенд `:5173` и бэкенд `:3001` работают — ✅
